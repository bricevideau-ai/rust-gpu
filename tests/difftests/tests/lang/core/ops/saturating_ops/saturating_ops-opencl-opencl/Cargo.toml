[package]
name = "saturating_ops-opencl-opencl"
edition.workspace = true

[lints]
workspace = true

# Common deps
[dependencies]
spirv-std.workspace = true
saturating_ops-shared = { path = "../saturating_ops-shared" }

# CPU deps
[target.'cfg(not(target_arch = "spirv"))'.dependencies]
difftest.workspace = true
