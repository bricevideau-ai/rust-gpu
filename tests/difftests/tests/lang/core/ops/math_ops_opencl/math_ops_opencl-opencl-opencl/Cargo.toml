[package]
name = "math_ops_opencl-opencl-opencl"
edition.workspace = true

[lints]
workspace = true

# Common deps
[dependencies]
spirv-std.workspace = true
math_ops_opencl-shared = { path = "../math_ops_opencl-shared" }

# CPU deps
[target.'cfg(not(target_arch = "spirv"))'.dependencies]
difftest.workspace = true
